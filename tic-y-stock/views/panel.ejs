<%- include('partials/head') %>
  <header class="topbar">
    <div class="topbar-inner">
      <div class="auth-logo">
        <svg class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
          <path d="M21 8a2 2 0 0 0-1-1.73l-7-4a2 2 0 0 0-2 0l-7 4A2 2 0 0 0 3 8v8a2 2 0 0 0 1 1.73l7 4a2 2 0 0 0 2 0l7-4A2 2 0 0 0 21 16Z" />
          <path d="m3.3 7 8.7 5 8.7-5" />
          <path d="M12 22V12" />
        </svg>
        <span>TIC &amp; Stock</span>
      </div>
      <div class="topbar-user">
        <span class="badge badge--<%= user.rol %>"><%= user.rol %></span>
        <span class="topbar-name"><%= user.nombre %></span>
        <a class="btn btn--ghost" href="/logout">Cerrar sesión</a>
      </div>
    </div>
  </header>
  <main class="placeholder">
    <section class="card placeholder-card">
      <h2>Panel del alumno — en construcción</h2>
      <p>La vista de consulta de stock en tiempo real llega en la próxima etapa (Sprint 3).</p>
      <p class="placeholder-session">Sesión iniciada como <strong><%= user.email %></strong>.</p>
    </section>
  </main>
</body>
</html>